version: 2

models:
  - name: customers
    description: One row per customer with their order history summary.
    columns:
      - name: customer_id
        description: Unique identifier for the customer.
        data_tests:
          - unique
          - not_null

      - name: first_name
        description: Customer's first name.

      - name: last_name
        description: Customer's last name.

      - name: first_order_date
        description: Date of the customer's first order, if they have placed an order.

      - name: most_recent_order_date
        description: Date of the customer's most recent order, if they have placed an order.

      - name: number_of_orders
        description: Total number of orders placed by the customer.
